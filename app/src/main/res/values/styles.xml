<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
		<item name="android:typeface">serif</item>
    </style>
    
	<style name="AppSplash" parent="android:Theme">
		<item name="android:windowBackground">@drawable/splash_drawable</item>
		<item name="android:windowNoTitle">true</item>
		<item name="android:scaleType">centerInside</item>
	</style>

    <style name="RadioButton">
        <item name="android:textColor">@color/radio_button_selected_color</item>
        <item name="android:minHeight">33dp</item>
        <item name="android:minWidth">70dp</item>
        <item name="android:gravity">center</item>
        <item name="android:button">@null</item>
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingRight">5dp</item>
    </style>
    　　
    <style name="Checkbox" parent="@android:style/Widget.CompoundButton.CheckBox">
     　　<item name="android:button">@drawable/check_drawable</item>　　
    　　<item name="android:paddingLeft">8dp</item>
    </style>
    
    <style name="WindowTopAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/window_vertical_top_in</item>  
        <item name="android:windowExitAnimation">@anim/window_vertical_top_out</item>
    </style>
    
    <style name="WindowBottomAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/window_vertical_bottom_in</item>  
        <item name="android:windowExitAnimation">@anim/window_vertical_bottom_out</item>
    </style>
    
    <style name="WindowCenterAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/window_center_in</item>  
        <item name="android:windowExitAnimation">@anim/window_center_out</item>
    </style>
    
    <style name="ProgressDialog" parent="android:Theme.Dialog">  
        <item name="android:windowFrame">@null</item>       <!-- Dialog的windowFrame框为无 -->  
        <item name="android:windowBackground">@android:color/transparent</item> <!-- 设置dialog的背景 -->  
        <item name="android:windowNoTitle">true</item>      <!-- 是否显示title -->  
        <item name="android:windowIsTranslucent">true</item>    <!-- 是否半透明 -->  
        <item name="android:windowIsFloating">true</item>       <!-- 是否浮现在activity之上 -->  
        <item name="android:windowContentOverlay">@null</item>  <!-- 也是个drawable，定义contentoverlay的背景 -->  
        <item name="android:backgroundDimEnabled">true</item>  <!-- 背景是否模糊显示 -->  
    </style>

</resources>
